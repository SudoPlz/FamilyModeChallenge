import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
  labelText: {
    color: 'white',
  },
  staticLabelText: {
    color: '#ADB3C555',
    top: -30,
    left: -10,
  },
});
export default styles;
